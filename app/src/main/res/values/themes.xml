<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Basic app theme; Compose will draw the actual UI on top of this -->
    <style name="Theme.FoodMart" parent="Theme.MaterialComponents.DayNight.NoActionBar.Bridge">
        <!-- Transparent status bar so Compose can control the surface -->
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>
